---
import BaseHead from "../components/layouts/BaseHead.astro";
import Navbar from "../components/layouts/navbar/Navbar.astro"; // <- updated import
import Footer from "../components/layouts/Footer.astro";
import { SITE_TITLE, SITE_DESCRIPTION } from "../consts";
---

<!doctype html>
<html lang="en">
  <head>
    <BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
  </head>
  <body style="min-height: 100vh; display: flex; flex-direction: column; margin: 0;">
    <Navbar /> <!-- replaced Header with Navbar -->
    <main>
      <h1>Under Construction</h1>
      <p>This is my personally crafted website. If you are reading this you are too early and much of the site is still being populated.</p>
      <p>Here are a few things I will be posting about:</p>
      <ul>
        <li>Self-hosting and running a home server</li>
        <ul>
          <li>Rolling your own modern data lakehouse</li>
          <li>Hosting apps for your family</li>
        </ul>
        <li>My experience as a part-time grad student in Georgia Tech's Online Masters School of Computer Science</li>
        <li>The odd data science project</li>
      </ul>

      <pre><code>
# This code is here for me to test css
function handleData(input):
    if not input:
        raise Error("Missing input")
    transformed = preprocess(input)
    for item in transformed:
        if item.isValid():
            result = compute(item)
            store(result)
        else:
            log("Invalid item", item)
    return summary(resultSet)
      </code></pre>
    </main>
    <Footer />
  </body>
</html>
