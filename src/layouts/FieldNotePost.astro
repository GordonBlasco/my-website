---
import type { CollectionEntry } from "astro:content";
import BaseHead from "../components/layouts/BaseHead.astro";
import Navbar from "../components/layouts/navbar/Navbar.astro";
import Footer from "../components/layouts/Footer.astro";
import FormattedDate from "../components/layouts/FormattedDate.astro";
import { Image } from "astro:assets";

type Props = CollectionEntry<"fieldNotes">["data"];

const { title, description, pubDate, updatedDate, heroImage } = Astro.props;
---

<!doctype html>
<html lang="en">
  <head>
    <BaseHead title={title} description={description} />
  </head>
  <body>
    <Navbar />
    <main class="article-main">
      {
        heroImage && (
          <div class="hero-image">
            <Image width={1020} height={510} src={heroImage} alt="" />
          </div>
        )
      }
      <article class="prose">
        <header class="article-title">
          <h1>{title}</h1>
          <hr />
          <div class="article-date"><FormattedDate date={pubDate} /></div>
          {
            updatedDate && (
              <div class="last-updated-on">
                Last updated on <FormattedDate date={updatedDate} />
              </div>
            )
          }
        </header>
        <slot />
      </article>
    </main>
    <Footer />
  </body>
</html>
